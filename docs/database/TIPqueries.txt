tables:

User
Answers
Survey
User-Survey

//I called all the TIP forms quizzes basically

//I noticed a lot of repetition in the original front end file, asking 

//Selects all faculty
SELECT user_name
FROM User
WHERE is_admin = 0

//Selects all divisions
SELECT faculty_division
FROM Users

//Selects all complete surveys
SELECT survey_data
FROM Survey
WHERE Survey.survey_status = 1

//SELECT survey_data
//FROM Survey
//WHERE Survey.survey_status = "in_progress"

SELECT survey_data
FROM Survey
WHERE Survey.survey_status = 0

//Selects the number of non-admin faculty in the database
SELECT COUNT(*)
FROM Users
WHERE asAdmin = 0

//Selects the number of admin faculty in the database
SELECT COUNT(*)
FROM Users
WHERE asAdmin = 1

//Selects the total number of users in the database
SELECT COUNT(*)
FROM Users

SELECT answer_data
FROM Answers

//SELECT User.quizAnswerID, Quizzes.quizstatus
//WHERE quizstatus = "complete", facultydivision = "anydivision"
//FROM Users
//INNER JOIN Quizzes ON Users.quizAnswerID = Quizzes.quizAnswerID

//Need to select questions that are not completed, or completed?  Impossible through database (if we don't save individual questions)

SELECT faculty_department, faculty_division
FROM Users

//Learning outcomes filtered by searching for the answer to learning outcome question in the quiz?

SELECT user_name
FROM User

SELECT survey_data
FROM Survey
WHERE survey_data LIKE '%anyword%

SELECT survey_data
FROM Survey
WHERE date = 2015 (or any date)

SELECT 

//Didn't do everything, but join format should be consistent mostly.  Haven't tested the queries, and still not sure about the
ERD so things could change.  
